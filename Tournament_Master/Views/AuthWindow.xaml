<Window x:Class="Tournament_Master.Views.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tournament Master" 
        Height="700" Width="1050"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        WindowState="Maximized"
        AllowsTransparency="True"
        Background="Transparent"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="AuthTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#EFE6D0"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Padding" Value="15,0,15,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFB300"/>
                                <Setter Property="Background" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AuthPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="#EFE6D0"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Padding" Value="15,0,15,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFB300"/>
                                <Setter Property="Background" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AuthComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Margin="4,2" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FFB300"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AuthComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#EFE6D0"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,0,10,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource AuthComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#EFE6D0" CornerRadius="10" BorderThickness="1" BorderBrush="#D1D5DB">
                                            <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="#4B5563" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0" Opacity="0.8"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left" />

                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" PopupAnimation="None">
                                <Border x:Name="DropDownBorder" 
                                        Background="#FFFFFF" 
                                        CornerRadius="12" 
                                        BorderThickness="1" 
                                        BorderBrush="#D1D5DB" 
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.1" Color="#000000"/>
                                    </Border.Effect>
                                    <StackPanel IsItemsHost="True" Margin="2"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AuthButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFB300"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="10">
                            <Grid>
                                <Border x:Name="HoverOverlay" Background="White" Opacity="0" CornerRadius="10"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="0.15" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.97" Duration="0:0:0.05"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.97" Duration="0:0:0.05"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Image Source="pack://application:,,,/Resources/Images/Background.jpg" Stretch="UniformToFill" />

        <Border Width="420" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Background="#FFFFFF" 
                CornerRadius="24" 
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="35" ShadowDepth="4" Opacity="0.15" Color="#000000"/>
            </Border.Effect>

            <Grid Margin="35,35,35,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="TOURNAMENT" FontSize="30" FontWeight="Black" Foreground="#111827" HorizontalAlignment="Center"/>
                    <TextBlock Text="MASTER" FontSize="14" FontWeight="Bold" Foreground="#FFB300" HorizontalAlignment="Center" Margin="0,-5,0,0"/>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,10">
                    <StackPanel x:Name="LoginPanel" Visibility="Visible" Opacity="1" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource m_Authorization}" FontSize="18" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                        <TextBlock Text="{DynamicResource m_Login}" Foreground="#374151" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="LoginField" Style="{StaticResource AuthTextBoxStyle}" TextChanged="Input_Changed" Margin="0,0,0,15"/>

                        <TextBlock Text="{DynamicResource m_Password}" Foreground="#374151" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="PasswordField" Style="{StaticResource AuthPasswordBoxStyle}" PasswordChanged="Input_Changed" Margin="0,0,0,8"/>

                        <TextBlock x:Name="StatusLabel" Foreground="#EF4444" FontSize="13" TextAlignment="Center" Margin="0,0,0,15" Height="18"/>

                        <Button Content="{DynamicResource m_SignIn}" Style="{StaticResource AuthButtonStyle}" Click="BtnLogin_Click"/>

                        <TextBlock Margin="0,15,0,0" HorizontalAlignment="Center">
                                <Run Text="{DynamicResource m_DontHaveAccount}" Foreground="#6B7280" FontSize="13"/>
                                <Hyperlink Click="ShowRegister_Click" Foreground="#FFB300" FontWeight="Bold" TextDecorations="None">
                                    <Run Text="{DynamicResource m_RegisterLink}"/>
                                </Hyperlink>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel x:Name="RegisterPanel" Visibility="Hidden" Opacity="0" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource m_CreateAccount}" FontSize="18" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                <TextBlock Text="{DynamicResource m_FirstName}" Foreground="#374151" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                                <TextBox x:Name="RegFirstName" Style="{StaticResource AuthTextBoxStyle}" Margin="0,0,0,12"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                <TextBlock Text="{DynamicResource m_LastName}" Foreground="#374151" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                                <TextBox x:Name="RegLastName" Style="{StaticResource AuthTextBoxStyle}" Margin="0,0,0,12"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="{DynamicResource m_Gender}" Foreground="#374151" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox x:Name="RegGender" Style="{StaticResource AuthComboBoxStyle}" Margin="0,0,0,12">
                            <ComboBoxItem Content="{DynamicResource m_Male}" IsSelected="True"/>
                            <ComboBoxItem Content="{DynamicResource m_Female}"/>
                        </ComboBox>

                        <TextBlock Text="{DynamicResource m_LoginHint}" Foreground="#374151" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="RegLogin" Style="{StaticResource AuthTextBoxStyle}" TextChanged="RegInput_Changed" Margin="0,0,0,12"/>

                        <TextBlock Text="{DynamicResource m_Password}" Foreground="#374151" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="RegPassword" Style="{StaticResource AuthPasswordBoxStyle}" PasswordChanged="RegInput_Changed" Margin="0,0,0,8"/>

                        <TextBlock x:Name="StatusLabelReg" Text="" FontSize="13" TextAlignment="Center" Margin="0,0,0,12" Height="18"/>

                        <Button Content="{DynamicResource m_SignUp}" Style="{StaticResource AuthButtonStyle}" Click="BtnDoRegister_Click"/>

                        <TextBlock Margin="0,15,0,0" HorizontalAlignment="Center">
                                <Run Text="{DynamicResource m_AlreadyHaveAccount}" Foreground="#6B7280" FontSize="13"/>
                                <Hyperlink Click="ShowLogin_Click" Foreground="#FFB300" FontWeight="Bold" TextDecorations="None">
                                    <Run Text="{DynamicResource m_SignIn}"/>
                                </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <Button Grid.Row="2" 
                        Content="{DynamicResource m_ExitApp}" 
                        Click="BtnExitApp_Click"
                        Margin="0,15,0,0"
                        Cursor="Hand"
                        FocusVisualStyle="{x:Null}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock x:Name="Txt" 
                                       Text="{TemplateBinding Content}" 
                                       Foreground="#DC2626" 
                                       FontSize="13" 
                                       FontWeight="SemiBold" 
                                       Opacity="0.7" 
                                       HorizontalAlignment="Center"
                                       TextDecorations="None"/>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Txt" Property="Opacity" Value="1"/>
                                    <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </Grid>
        </Border>
    </Grid>
</Window>